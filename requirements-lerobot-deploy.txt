# LeRobot AMP deploy environment (Python 3.12)
#
# IMPORTANT: this stack uses the `lerobot.rollout` framework and
# `lerobot.utils.import_utils.require_package`, which are NOT in any PyPI
# release (latest is 0.5.1). They only exist on official lerobot `main`.
# Do NOT use `lerobot>=0.4.0` from PyPI — it will fail with
# `ImportError: cannot import name 'require_package'`.
#
# The lerobot[unitree_g1] extra does NOT install the Unitree SDK, but
# AmpG1Robot.__init__ requires `unitree_sdk2py` even for sim2sim. Install it
# manually (not on PyPI), e.g.:
#   git clone https://github.com/unitreerobotics/unitree_sdk2_python.git
#   pip install -e ./unitree_sdk2_python
#
# Install:
#   pip install -r requirements-lerobot-deploy.txt
#   pip install -e packages/lerobot_policy_amp_velocity -e packages/lerobot_robot_amp_g1

# Official lerobot from git, pinned to a known-good commit for reproducibility.
# Bump the @<sha> as needed (or use @main to always track latest).
lerobot[unitree_g1] @ git+https://github.com/huggingface/lerobot.git@49755a3d9e7d43ae93092de8324e75348955afab

onnxruntime>=1.17.0
numpy>=1.26.0
pyyaml>=6.0
pynput>=1.7.0
torch>=2.2.0
